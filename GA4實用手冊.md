# 波波酪梨 GA4 實用手冊

> 先講結論：GA4 有九成的功能跟你無關。
> **你只需要用四個地方**，其餘的看到就跳過。
>
> 本文分三部分：
> 1. 開賣前一次性設定（有兩項無法回溯，一定要先做）
> 2. GA4 的四個地方，各自什麼時候用
> 3. 五張你會反覆用到的報表，照著點就好

---

# 第一部分：開賣前一次性設定

## ⚠️ 1-1 資料保留期限（最容易漏、後果最嚴重）

GA4 預設只保留 **2 個月**的明細資料。超過就只剩彙總數字，
你想比較「這場跟上一季那場」時會發現資料已經沒了。

**管理 → 資料顯示 → 資料保留 → 事件資料保留 → 改成「14 個月」→ 儲存**

免費版最長就是 14 個月。**改之前的資料不會回溯**，所以現在就去改。

---

## ⚠️ 1-2 自訂定義（不登記的話報表拉不出來）

你的程式已經在送 `fail_reason`、`browser_env` 這些參數了，
但 GA4 不會自動讓它們變成可用的報表欄位——**必須手動登記**。

而且**登記之前送的資料無法回溯**，所以要在開賣前設好。

**位置：管理 → 資料顯示 → 自訂定義 → 建立自訂維度**

範圍一律選「**事件**」。事件參數名稱**必須完全一致**（大小寫、底線都要對）。

### 必做的六個維度

| 維度名稱（自己取，會顯示在報表上） | 事件參數（必須一字不差） | 回答什麼問題 |
|---|---|---|
| 失敗原因 | `fail_reason` | 客人是卡在鎖逾時、售罄、還是無法確認？ |
| 訂單來源 | `order_source` | 這筆是前端送的還是後端補的？ |
| 救回來源 | `recovery_source` | 救回是發生在重試前、還是逾時後？ |
| 瀏覽器環境 | `browser_env` | LINE 流量佔多少？轉換率有沒有比較差？ |
| 送出嘗試次數 | `attempt_no` | 客人平均按幾次才成功？ |
| 流程步驟 | `step_name` | 客人走到哪一頁就離開了？ |

### 選做的三個指標

**建立自訂指標**（不是維度），範圍同樣選「事件」：

| 指標名稱 | 事件參數 | 測量單位 |
|---|---|---|
| 庫存總數 | `stock_total` | 標準 |
| 購物車金額 | `cart_value` | 標準 |
| 品項數 | `item_count` | 標準 |

> 維度 vs 指標的差別：**維度是「切法」，指標是「數字」**。
> 「按失敗原因分組」→ 維度；「平均購物車金額」→ 指標。

---

## 1-3 排除你自己的測試流量（建議）

你每次改版都會自己逛好幾遍，那些會混進報表裡。

**管理 → 資料收集和修改 → 資料篩選**

預設已經有一個「Internal Traffic」篩選器，但狀態是「測試中」（不會真的過濾）。
要它生效需要兩步：

1. **管理 → 資料串流 → 點串流 → 設定代碼 → 顯示更多 → 定義內部流量**
   → 建立規則，填入你家/工作室的對外 IP（Google 搜「what is my ip」就看得到）
2. 回到資料篩選，把那個篩選器狀態改成「**有效**」

如果你常換網路（手機測試），這步的效益有限，跳過也可以——
反正你的測試量相對真實開賣的流量很小。

---

# 第二部分：GA4 的四個地方

介面上那一大堆選單，你只要記住這四個。

## ① DebugView — 驗證埋點用

**管理 → DebugView**

即時顯示事件流，用來確認「我剛剛做的動作有沒有被記錄到」。

**開啟方法**：F12 Console 執行
```javascript
gtag('config', 'G-99EP460CDY', { debug_mode: true });
```

**什麼時候用**：改完程式碼、想確認事件有沒有正確送出時。
平常不用開。

---

## ② 即時報表 — 開賣當下瞄一眼

**報表 → 即時**

只涵蓋**最近 30 分鐘**。

> 🚨 **不要拿它當秒殺當下的儀表板。**
>
> 你的搶購是幾分鐘內結束的事，而 GA4 即時報表有數十秒延遲、
> 數字還會跳動。開賣那五分鐘請看你的 **Firebase 和「戰況統計」**——
> 那才是即時且準確的。
>
> 即時報表的用途是「確認追蹤還活著」，不是決策依據。

---

## ③ 標準報表 — 事後看營收與流量

**報表 → 營利 → 電子商務購買**（看營收、商品成效）
**報表 → 客戶開發 → 流量開發**（看客人從哪來，UTM 就是看這裡）

**有 24～48 小時延遲**，所以開賣隔天再看。

---

## ④ 探索 — 真正回答問題的地方

**左側選單「探索」→ 空白 → 自由形式**

標準報表只給你 Google 預設的組合，**你自訂的那些事件與參數要在這裡才拉得出來**。
第三部分的五張報表全部在這裡做。

---

## 一個你必須先建立的心智模型

GA4 的一切都是這個公式：

```
報表 = 維度（怎麼切） × 指標（數字是什麼）
```

- **維度**：失敗原因、瀏覽器環境、流程步驟…（文字，用來分組）
- **指標**：事件計數、使用者數、營收…（數字，用來加總）

在「探索」裡，左欄把維度拖到「列」，把指標拖到「值」，報表就出來了。
**只有這一招，但這一招能做出你需要的所有報表。**

---

# 第三部分：五張你會反覆用到的報表

每張都是「探索 → 空白 → 自由形式」，然後照著設定。

---

## 📊 報表一：失敗原因分布（最重要）

**回答**：客人下不了單的時候，到底卡在哪裡？

| 設定項 | 拖什麼進去 |
|---|---|
| 維度（列） | 失敗原因 |
| 指標（值） | 事件計數 |
| 篩選器 | 事件名稱 **完全比對** `order_submit_fail` |

**怎麼讀**：

| 看到什麼 | 代表什麼 | 該做什麼 |
|---|---|---|
| `lock_timeout` 佔大宗 | 併發超過 GAS 承受力 | 下次別再加量，或考慮分批開賣 |
| `timeout_unconfirmed` 多 | Google 壅塞回 HTML 錯誤頁 | 看報表三，確認救回率 |
| `sold_out` 多 | 純粹是搶不到 | 這是好事，代表需求大於供給 |
| `order_closed` 出現 | 有人在你關單後還在送 | 正常，煞車有生效 |

---

## 📊 報表二：漏斗流失（客人在哪一步走掉）

**回答**：進站的人有多少走到最後？在哪一頁掉最多？

| 設定項 | 拖什麼進去 |
|---|---|
| 維度（列） | 流程步驟 |
| 指標（值） | 事件計數、使用者總數 |
| 篩選器 | 事件名稱 **完全比對** `funnel_step` |

理想的數字會像階梯往下遞減：

```
公告      1000 人
品種介紹   620 人   ← 掉了 38%
價目表     540 人
訂購表單   410 人
完成頁      95 人   ← 這一段的落差就是你的改善空間
```

**如果「訂購表單 → 完成頁」掉得特別兇**，交叉看報表一就知道是庫存不夠、
還是系統擋住了。這兩件事的處理方式完全相反。

---

## 📊 報表三：救回率（你最想知道的那個數字）

**回答**：有多少客人以為自己失敗了，其實訂單成立了？

| 設定項 | 拖什麼進去 |
|---|---|
| 維度（列） | 事件名稱 |
| 指標（值） | 事件計數 |
| 篩選器 | 事件名稱 **完全比對**（可多選）：`order_submit_attempt`、`order_recovered`、`purchase` |

**算法**：
```
救回率 = order_recovered ÷ purchase
```

壓力測試時推估約 25%。這是第一次拿到**真實流量下的數字**。

再把維度換成「救回來源」，可以細分：
- `retry_precheck` — 客人重按送出時，前端先查到收據（v5 修的那條死路）
- `timeout_recovery` — 送出後逾時，等待輪詢查到收據

---

## 📊 報表四：LINE 流量的真面目

**回答**：LINE 客人的行為跟一般瀏覽器有沒有差？

| 設定項 | 拖什麼進去 |
|---|---|
| 維度（列） | 瀏覽器環境 |
| 指標（值） | 使用者總數、事件計數、交易次數 |

**要特別留意的陷阱**：

如果 `line_inapp` 的「新使用者比例」接近 100%，**那多半不是真的**。
LINE 的 in-app 瀏覽器對 storage 處理不穩定，`client_id` 可能每次都重新產生
→ 同一個人被算成很多個新使用者。

所以 LINE 這一列的**使用者數會被高估、回訪率會被低估**。
拿它做行銷判斷前先記住這件事。

---

## 📊 報表五：開賣瞬間的守候人數

**回答**：開賣前有多少人在等？真正動手的有幾個？

| 設定項 | 拖什麼進去 |
|---|---|
| 維度（列） | 事件名稱 |
| 指標（值） | 使用者總數 |
| 篩選器 | 事件名稱 **完全比對**：`sale_page_ready`、`sale_open`、`order_submit_attempt`、`purchase` |

```
sale_page_ready       380 人   ← 開賣前已經在頁面上等
sale_open             290 人   ← 撐到倒數歸零還在的
order_submit_attempt  180 人   ← 真的按下送出的
purchase               95 人   ← 成功的
```

**`sale_page_ready → sale_open` 的落差**是「等不及先走了」的人，
如果這段掉很多，代表你的預告時間太早或倒數體驗不夠吸引人。

---

# 開賣日操作腳本

## 開賣前一天

- [ ] `開賣前體檢()`（第 8 項會檢查 GA4）
- [ ] `重設戰況統計()`
- [ ] 確認自訂定義六個維度都已建立
- [ ] 確認資料保留改成 14 個月

## 開賣當下（那五分鐘）

**看 Firebase 和「戰況統計」，不要看 GA4。**

GA4 有延遲、即時報表只有 30 分鐘窗口，開賣當下什麼都看不準。

## 開賣後 1 小時

- [ ] `檢查戰況()` — 拿到鎖逾時、救回、重複攔截的即時數字
- [ ] `檢查GA4狀態()` — 確認補送都完成了（P 欄應該都是「已送出」）

## 開賣後隔天（資料完整了）

- [ ] **對帳**：GA4 交易次數 vs 試算表訂單筆數
  - 相同 → 去重正常
  - GA4 明顯偏多 → 執行 `停用GA4補送()`，改成只靠前端
- [ ] 跑一遍報表一～五
- [ ] 把數字記下來，下一場才有得比

---

# 常見疑惑

**Q：為什麼我看不到剛剛的測試訂單？**
標準報表有 24～48 小時延遲。要即時確認請用 DebugView 或即時報表。

**Q：「使用者總數」跟「工作階段」差在哪？**
使用者 = 幾個人；工作階段 = 幾次造訪。同一個人今天來三次 = 1 使用者 / 3 工作階段。
秒殺情境下看「使用者」比較有意義。

**Q：探索報表設定好了，下次要重做嗎？**
不用，會自動存在「探索」清單裡，改個名字就是你的常用報表了。

**Q：報表數字跟試算表對不起來？**
正常的小幅差異（±5%）來自廣告阻擋器、客人關掉頁面太快等，
不用追究。差異超過 20% 才需要查。

**Q：那 49 筆「無CID」的舊訂單要處理嗎？**
不用。它們是改版前的訂單，GA4 那邊沒有對應的使用者可歸屬。
標記後就不再被掃描，不會變成處理不完的待辦。
